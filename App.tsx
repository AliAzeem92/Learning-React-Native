import React, {useState} from 'react';
import {Button, Text, View} from 'react-native';

const App = () => {
  return (
    <View>
      <Text
        style={{
          textAlign: 'center',
          fontSize: 30,
          backgroundColor: 'lightblue',
          color: 'red',
        }}>
        Styling
      </Text>
    </View>
  );
};

export default App;
